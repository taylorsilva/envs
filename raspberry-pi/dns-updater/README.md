Create a file in this directory called `.ddns-password` with the password for updating DNS with Namecheap.

```
password=<the password>
```

Create a cronjob to this script at this directory: `crontab -e`
